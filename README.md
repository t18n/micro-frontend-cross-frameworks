# Micro-frontend cross-frameworks

A repo to demonstrate the cross-platform nature of micro-frontends.

## Getting started

- Terminal 1: Start remote app
  ```sh
  cd remote-solid
  yarn install
  yarn start:live
  ```

- Terminal 2: Start react-host app
  ```sh
  cd react-host
  yarn install
  yarn start:live
  ```
